import puppeteer from 'puppeteer';

export const webScrapper = async (baseUrl: string = "https://www.sreality.cz/en/search/for-sale/apartments?page=", number: number = 500) => {
    console.log('webScrapper');
}
